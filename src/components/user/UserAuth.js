import React from 'react'

const UserAuth = ({ children }) => {
  return (
    <div>
        <div className="main-wrapper">
            {children}
        </div>
    </div>
  )
}

export default UserAuth